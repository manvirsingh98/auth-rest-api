<div class="jumbotron jumbotron-fluid py-5">
    <div class="container">
        <h1 class="display-4">{{getUser.name}}</h1>
        <p class="lead">{{getUser.email}}</p>
    </div>
</div>
<div class="container">
        {{!-- <form action="/update" method="put">
                <h4>Header</h4>
                        <div class="form-group">
                            <label for="exampleInputEmail1">Title</label>
                            <input type="text" class="form-control" id="title" value="{{getData.title}}">
                        </div>
                        <div class="form-group">
                            <label for="exampleFormControlTextarea1">Example textarea</label>
                            <input class="form-control" id="exampleFormControlTextarea1" value=" {{getData.desc}}">
                           
                        </div>
                        <button type="submit" class="btn btn-primary">Update</button>
                    </form> --}}
        <!-- /.col-md-8 -->
         <form >
<div class="row">
           
    {{#each getData}}
    <div class="col-12 col-sm-6 col-lg-4 my-3">
                    <div class="card shadow">
                        <div class="card-header">
                            Portfolio
                        </div>
                        <div class="card-body">
                            <h4 class="card-title">{{this.title}}</h4>
                            <h6 class="card-subtitle mb-2">{{this.desc}}</h6>
                            <p class="card-text">{{this.date}} </p>
                            <a href="/profile/{{this._id}}" class="btn btn-danger">Edit</a>
                        </div>
                    </div>
                </div>
                 {{/each}}
               
</div>

  </form>

</div>